import React from 'react';
import ButtonToggleTheme from './index';

export default { title: 'Button Toggle Theme' };

export const Default = () => <ButtonToggleTheme />;

export const isDarkMode = () => <ButtonToggleTheme isDarkMode />;
